class Solution {
public:
    int countCommas(int n) {
        string temp=to_string(n);
        int len=temp.length();
        if(len<=3)
        {
            return 0;
        }
        return (n-999);
    }
};
